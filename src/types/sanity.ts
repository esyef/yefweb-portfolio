export type SKILLS = {
  name: string;
  bgColor: string;
  icon: string;
};

export type PROJECTS = {
  title: string;
  description: string;
  projectLink: string;
  codeLink: string;
  imgUrl: string;
  tags: string[];
};
